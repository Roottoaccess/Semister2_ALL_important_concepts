let div = document.querySelector("div");
console.dir(div.innerText) // Only the text

console.dir(div.innerHTML) // html content will also come
// Next is the innerText

let old_heading = document.querySelector("h1")

